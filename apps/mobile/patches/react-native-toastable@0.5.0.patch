diff --git a/src/components/ToastableWrapper.tsx b/src/components/ToastableWrapper.tsx
index 75fb74e..47c69de 100644
--- a/src/components/ToastableWrapper.tsx
+++ b/src/components/ToastableWrapper.tsx
@@ -229,10 +229,10 @@ const isSwipeDirectionAllowed = ({
 };

 const ANIMATION_MAP: Record<SwipeDirection, AnimatableViewAnimation> = {
-  up: 'slideOutUp',
-  down: 'slideOutDown',
+  up: 'slideOutLeft',
+  down: 'slideOutLeft',
   left: 'slideOutLeft',
-  right: 'slideOutRight',
+  right: 'slideOutLeft',
 };

 export const ToastableWrapper = ({
@@ -264,7 +264,7 @@ export const ToastableWrapper = ({
     if (interactionHandle == null) {
       interactionHandle = InteractionManager.createInteractionHandle();
     }
-    content.animate('slideInDown', props.animationInTiming).then(() => {
+    content.animate('slideInUp', props.animationInTiming).then(() => {
       isTransitioning = false;

       if (interactionHandle) {
