# Template sync

This template was generated using Povio's `react-native-template`. If you want to update your repo with the changes from the template, run this:

```bash
git remote add template git@github.com:poviolabs/react-native-template.git
git fetch --all

git merge template/main --allow-unrelated-histories
```
